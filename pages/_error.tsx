import type { AppProps } from "next/app";

export default function App({ Component, pageProps }: AppProps) {
  return <div>
    <h1>Not Found - PAGES ROUTER</h1>
    <p>This is a custom not found page added to the PAGES router</p>
  </div>
}
